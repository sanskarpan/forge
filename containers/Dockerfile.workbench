# Frontend validation image. Dependencies are installed at container runtime
# because CI mounts the checked-out repository over /workspace.
FROM node:22-bookworm-slim

WORKDIR /workspace

CMD ["sh", "-c", "npm ci --prefix workbench --ignore-scripts && npm test --prefix workbench && npm run build --prefix workbench"]
