# Lightweight frontend validation image. The current workbench has no
# runtime dependency install, but this image is ready for the React/Vite
# dependency set as that scope lands.
FROM node:22-bookworm-slim

WORKDIR /workspace

CMD ["npm", "test", "--prefix", "workbench"]
